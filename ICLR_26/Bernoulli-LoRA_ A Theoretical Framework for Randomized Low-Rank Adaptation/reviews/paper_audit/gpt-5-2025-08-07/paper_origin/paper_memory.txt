# Global Summary
- Problem: Provide a unified, theoretically grounded framework for parameter-efficient fine-tuning (PEFT) via low-rank adaptation (LoRA), addressing gaps in convergence theory, stochastic variance reduction, and federated learning (FL) with communication compression.
- Core approach: Bernoulli-LoRA randomly chooses, at each step, to optimize either A or B in a rank-r LoRA update using a Bernoulli trial with probability p. The fixed counterpart is sampled from a specified distribution (Left/Right sketches). Each update equals a projected gradient step in the full parameter space: W^{t+1} = W^t − γ Ĝ^t with Ĝ^t = H_B^t G^t w.p. p, or G^t H_A^t w.p. 1−p. The framework instantiates multiple base estimators G^t (full, stochastic, VR, or FL-compressed).
- Theoretical scope: Convergence guarantees under L-smooth non-convex objectives, PL condition, and non-smooth convex objectives (with both constant and Polyak stepsizes). Assumption 1 (positive expected projection) holds for common Gaussian sketches with E[H] = (r/n) I and λ_min = r/n.
- Algorithms analyzed: Bernoulli-LoRA-GD, -SGD, -MVR, -PAGE; and FL variants Fed-Bernoulli-LoRA-QGD, -MARINA, -EF21. Convergence rates summarized (notation Δ^0 = f(W^0) − f*, 𝒢^0 = ||G^0 − ∇f(W^0)||_F^2, Ĝ^0 = (1/M)∑||G_l^0 − ∇f_l(W^0)||_F^2, λ_min = p λ_min^{HB} + (1−p) λ_min^{HA}, λ_max analogously):
  - GD (non-convex): Δ bound in gradients: E||∇f(Ẇ^T)||_F^2 ≤ 2Δ^0/(γ λ_min T).
  - SGD (non-convex): ≤ Δ term 6Δ^0/(γ λ_min T) + variance floor γ L C1 (λ_max/λ_min). Under PL: linear rate (1 − γ μ λ_min/2)^T Δ^0 + (γ L C1/μ) (λ_max/λ_min).
  - MVR (non-convex): ≤ 2Δ^0/(γ λ_min T) + (𝒢^0/(bT) + 2bσ^2/(2−b)) (λ_max/λ_min).
  - PAGE (non-convex): ≤ 2Δ^0/(γ λ_min T) + (𝒢^0/(qT)) (λ_max/λ_min).
  - Fed-QGD (non-convex): ≤ 6Δ^0/(γ λ_min T) + (2γ L ω Δ*/M) (λ_max/λ_min); PL summary table states additive term (γ L^2 ω λ_max)/(M μ λ_min).
  - Fed-MARINA (non-convex): ≤ 2Δ^0/(γ λ_min T) + (𝒢^0/(qT)) (λ_max/λ_min).
  - Fed-EF21 (non-convex): ≤ 2Δ^0/(γ λ_min T) + (2 Ĝ^0/(βT)) (λ_max/λ_min).
  - Non-smooth convex (GD): with E[H] = α I, constant stepsize: E[f(Ŵ) − f(W*)] ≤ R_0^2/(2 γ α T) + γ L_0^2/2; optimal γ* = √(R_0^2/(T α L_0^2)) yields R_0 L_0/√(α T); Polyak stepsize achieves ≤ R^0 L_0/√(α T).
- Empirical scope and highlights:
  - Linear regression with non-convex regularization (n = 4096; pretrain m̃ = 90,000; fine-tune m̂ = 10,000; L̂ ≈ 12,305.3): In the stochastic setting (batch = 100 = 1% of data), Bernoulli-LoRA-PAGE markedly reduces variance and converges to target tolerance (gradient norm squared drops from ~10^3 to <10^−15 over data passes), while SGD variants (RAC-LoRA-SGD A/B and Bernoulli-LoRA-SGD) stall; step sizes γ = c/L̂ with c tuned per method.
  - MNIST MLP transfer (pretrain on digits 0–4, adapt to 5–9), r = 1, 50 epochs, AdamW (β1 = 0.9, β2 = 0.999, ε = 1e−8), lr = 2e−4, batch 128, 20 seeds, median±std:
    - FPFT: 99.5 (54,700 params).
    - LoRA (Gaussian/Zero): 85.69 ± 1.60 (1K), LoRA (Zero/Gaussian): 89.82 ± 0.90 (1K).
    - COLA: 93.32 ± 0.50 (G/Z) and 96.55 ± 0.20 (Z/G), 1K.
    - AsymmLoRA: 64.04 ± 6.90 (G/Z, 133) and 74.52 ± 7.20 (Z/G, 912).
    - RAC-LoRA: 93.02 ± 0.50 (G/Z, 133) and 96.49 ± 0.20 (Z/G, 912).
    - Bernoulli-LoRA: 96.46 ± 0.17 with p = 0.99, expected trainable ≈ p·912 + (1−p)·133 ≈ 904; used deterministic A/B assignment at initialization despite the probabilistic prescription.
- Caveats explicitly stated: Full gradient methods are impractical at large scale (used for theory). Some stepsize constants and bounds rely on assumptions (Expected Smoothness, bounded variance, PL). Practicality of Assumption 1 is argued via Gaussian sketches (E[H] = (r/n) I). Detailed hyperparameter sweep ranges or computational budgets outside those reported are not specified.

# Abstract
- Motivation: PEFT is vital for adapting large models efficiently; LoRA is effective but lacks comprehensive theory. RAC-LoRA offered early analysis.
- Proposal: Bernoulli-LoRA, a probabilistic framework that flips a Bernoulli coin each step to decide whether to update A or B; other matrix is sampled and fixed.
- Scope: Variants include Bernoulli-LoRA-GD, -SGD, -PAGE, -MVR, and FL variants -QGD, -MARINA, -EF21.
- Theory: Convergence guarantees for each variant under standard non-convex assumptions; extended to convex, non-smooth with constant and Polyak-type stepsizes.
- Empirics: Extensive experiments validating theory; demonstrates practical efficacy.

# Introduction
- Background: PEFT updates a small subset of parameters; LoRA parameterizes ΔW = (α/r) B A added to W^0. Typically A is Gaussian-initialized and B zero-initialized; r ≪ min{m, n}; α acts as stepsize. LoRA reduces parameters and mitigates forgetting.
- COLA: Sequentially trains chains of low-rank modules to approximate higher-rank updates W = W^0 + (α/r) ∑_{t=0}^{T−1} B^t A^t, improving on non-low-rank adaptations without direct high-rank fitting.
- Contributions:
  - Bernoulli-LoRA: Randomized, generic framework choosing A or B to train via a Bernoulli trial; other factor drawn from a distribution and fixed that step. Generalizes existing LoRA update strategies and operates sequentially over T low-rank updates.
  - Foundational variants: Bernoulli-LoRA-GD (full gradients) and -SGD (unbiased stochastic gradient).
  - Variance reduction in non-convex settings: Bernoulli-LoRA-PAGE (finite-sum; PAGE estimator) and -MVR (expectation; STORM-inspired momentum VR).
  - Federated Learning extensions: Fed-Bernoulli-LoRA-QGD (QSGD-style quantization), -MARINA (compressed gradient differences), -EF21 (error feedback with contractive compressors). Claimed as first comprehensive theoretical analyses of LoRA-type methods with communication-efficient FL techniques.
  - Non-smooth convex analysis: First theoretical analysis for LoRA-type methods in this class; rates for constant and Polyak stepsizes.
- Summary table of convergence (non-convex “NC” and PL “PŁ” rates; constants omitted):
  - Bernoulli-LoRA-GD: NC Δ^0/(γ λ_min T); PL (1 − γ μ λ_min)^T Δ^0.
  - Bernoulli-LoRA-SGD: NC Δ^0/(γ λ_min T) + γ L C1 λ_max/λ_min; PL (1 − γ μ λ_min)^T Δ^0 + γ L C1/(μ) · λ_max/λ_min.
  - Bernoulli-LoRA-MVR: NC Φ1/(γ λ_min T) + [b σ^2 λ_max]/[(2−b) λ_min]; PL (1 − γ μ λ_min)^T Φ1 + [b σ^2 λ_max]/[(2−b) μ λ_min]; Φ1 := Δ^0 + (b γ/(2−b)) 𝒢^0.
  - Bernoulli-LoRA-PAGE: NC Φ2/(γ λ_min T); PL (1 − γ μ λ_min)^T Φ2; Φ2 := Δ^0 + (γ/q) 𝒢^0.
  - Fed-Bernoulli-LoRA-QGD: NC Δ^0/(γ λ_min T) + γ L ω Δ* λ_max/(M λ_min); PL (1 − γ μ λ_min)^T Δ^0 + γ L^2 ω λ_max/(M μ λ_min).
  - Fed-Bernoulli-LoRA-MARINA: NC Φ2/(γ λ_min T); PL (1 − γ μ λ_min)^T Φ2.
  - Fed-Bernoulli-LoRA-EF21: NC Φ3/(γ λ_min T); PL (1 − γ μ λ_min)^T Φ3; Φ3 := Δ^0 + [γ/(1−√(1−β))] 𝒢^0.
  - Notation: λ_min = p λ_min^{HB} + (1−p) λ_min^{HA}, λ_max analogously; q is PAGE frequency; β contractive compressor parameter; b momentum; ω compression; Δ* heterogeneity gap; T chain length.

# Preliminaries
- Problem: Minimize f(W^0 + ΔW) over ΔW ∈ ℝ^{m×n}.
- Stochastic settings:
  - Finite-sum: f(W^0 + ΔW) = (1/N) ∑_{i=1}^N f_i(W^0 + ΔW).
  - Expectation: f(W^0 + ΔW) = E_{ξ∼𝔻}[f_ξ(W^0 + ΔW)].
- Federated objective: f(W^0 + ΔW) = (1/M) ∑_{l=1}^M f_l(W^0 + ΔW).
- Notation:
  - Frobenius norm and trace inner product; Δ^0 := f(W^0) − f*, 𝒢^0 := ||G^0 − ∇f(W^0)||_F^2, Ĝ^0 := (1/M)∑ ||G_l^0 − ∇f_l(W^0)||_F^2.
  - Pseudoinverse †.

# Motivation
- Gaps: LoRA reparameterization can render smooth f non-smooth in (A, B), complicating analysis. COLA’s analysis optimizes ΔW in full rank, not modeling the low-rank core. Many LoRA methods remain heuristic and sensitive to hyperparameters; divergence examples exist (e.g., COLA).
- RAC-LoRA initiated theory but lacks optimal VR analyses for non-convex settings and coverage of advanced FL setups with compression and error feedback.
- Need: A unified, theoretically justified framework that handles stochastic training, VR, and FL communications constraints for low-rank updates.

# Method
- Framework (Algorithm 1):
  - At step t, sample c^t ∼ Be(p).
  - Left sketch: sample B_S^t ∼ 𝒟_B, optimize Â^t in ΔW = (α/r) B_S^t Â^t.
  - Right sketch: sample A_S^t ∼ 𝒟_A, optimize ̂B^t in ΔW = (α/r) ̂B^t A_S^t.
- Projected update equivalence:
  - With one GD step on the subproblem, Left sketch yields W^{t+1} = W^t − γ H_B^t ∇f(W^t), with H_B^t = B_S^t ((B_S^t)^⊤ B_S^t)^† (B_S^t)^⊤.
  - Right sketch yields W^{t+1} = W^t − γ ∇f(W^t) H_A^t, with H_A^t = (A_S^t)^⊤ (A_S^t (A_S^t)^⊤)^† A_S^t.
  - Unified form: W^{t+1} = W^t − γ Ĝ^t with Ĝ^t = H_B^t G^t (w.p. p) or G^t H_A^t (w.p. 1−p), G^t a base estimator.
- Variants and base estimators (Table 2):
  - GD: G^t = ∇f(W^t).
  - SGD: G^t = unbiased stochastic gradient g(W^t).
  - MVR: G^{t} = ∇f_{ξ^t}(W^t) + (1−b)(G^{t−1} − ∇f_{ξ^t}(W^{t−1})).
  - PAGE (finite-sum): G^t = ∇f(W^t) w.p. q; else G^{t−1} + ∇f_{i_t}(W^t) − ∇f_{i_t}(W^{t−1}).
  - Fed-QGD: G^t = (1/M) ∑_l 𝒬_l^t(∇f_l(W^t)) (unbiased compressor with variance parameter ω).
  - Fed-MARINA: per-client G_l^t uses full gradient w.p. q or adds compressed gradient difference; server averages.
  - Fed-EF21: per-client G_l^t = G_l^{t−1} + 𝒞_l^t(∇f_l(W^t) − G_l^{t−1}) (contractive compressor with parameter β); server averages.
- Assumptions:
  - Assumption 1 (Positive Expected Projection): λ_min(E[H]) > 0. Gaussian sketches imply E[H] = (r/n) I (Appendix B), so λ_min = r/n.
  - Assumption 2: f is lower bounded.
  - Assumption 3: L-Lipschitz gradient (smoothness).
  - Assumption 4 (Expected Smoothness for SGD): E||g(W)||^2 ≤ 2A1(f(W) − f*) + B1||∇f(W)||^2 + C1.
  - Assumption 5 (Bounded Variance): E||∇f_ξ(W) − ∇f(W)||^2 ≤ σ^2.
  - Assumption 6 (PŁ): (1/2)||∇f(W)||^2 ≥ μ(f(W) − f*).
  - Non-smooth convex regime: convex, L_0-Lipschitz; and E[H] = α I.
- Convergence highlights (step-size conditions provided in-section; here are bounds):
  - Theorem 1 (GD, non-convex): E||∇f(Ẇ^T)||^2 ≤ 2Δ^0/(γ λ_min^p T) with 0 < γ ≤ 1/L.
  - Theorem 2 (SGD, non-convex): E||∇f(Ẇ^T)||^2 ≤ 6Δ^0/(γ λ_min^p T) + γ L C1 (λ_max^p/λ_min^p), with γ bounded by problem constants (min of terms depending on L, A1, B1, λ_min^p, λ_max^p, T).
  - Theorem 3 (MVR, non-convex): E||∇f(Ẇ^T)||^2 ≤ 2Δ^0/(γ λ_min^p T) + (𝒢^0/(bT) + 2bσ^2/(2−b)) (λ_max^p/λ_min^p).
  - Theorem 4 (PAGE, non-convex): E||∇f(Ẇ^T)||^2 ≤ 2Δ^0/(γ λ_min^p T) + (𝒢^0/(qT)) (λ_max^p/λ_min^p).
  - Theorem 5 (Fed-QGD, non-convex): E||∇f(Ẇ^T)||^2 ≤ 6Δ^0/(γ λ_min^p T) + (2γ L ω Δ*/M) (λ_max^p/λ_min^p).
  - Theorem 6 (Fed-MARINA, non-convex): E||∇f(Ẇ^T)||^2 ≤ 2Δ^0/(γ λ_min^p T) + (𝒢^0/(qT)) (λ_max^p/λ_min^p).
  - Theorem 7 (Fed-EF21, non-convex): E||∇f(Ẇ^T)||^2 ≤ 2Δ^0/(γ λ_min^p T) + (2Ĝ^0/(βT)) (λ_max^p/λ_min^p).
  - Theorem 8 (SGD under PŁ): E[f(W^T) − f*] ≤ (1 − γ μ λ_min^p/2)^T Δ^0 + (γ L C1/μ) (λ_max^p/λ_min^p).
  - Non-smooth convex (Theorem 10): constant stepsize rate R_0^2/(2γ α T) + γ L_0^2/2; with γ* gives R_0 L_0/√(α T); Polyak stepsize matches R^0 L_0/√(α T).

# Experiments
- 8.1 Linear Regression with Non-convex Regularization:
  - Pretraining problem (n = 4096, m̃ = 9×10^4): minimize 1/(2 m̃) ||D̃ x − b̃||^2 + λ̃ ∑ x_j^2/(1 + x_j^2); λ̃ = ||D̃||_2 ≈ 18.2; L̃ ≈ 54.7; optimize to ||∇f̃(x̃*)||^2 ≤ 10^−8.
  - Fine-tuning problem (n = 4096, m̂ = 10^4): same objective form; λ̂ = ||D̂||_2 ≈ 4101.7; L̂ ≈ 12305.3; initialization x̃*.
  - Stochastic setting: batch size 100 (1% of data). Compared RAC-LoRA-SGD (A trains B after resampling A; B trains A analogously), Bernoulli-LoRA-SGD, Bernoulli-LoRA-PAGE; all methods used γ = c/L̂ with c tuned per method.
  - Result (Figure 1): Bernoulli-LoRA-PAGE (e.g., p = 0.5) reduces ||∇f(x)||^2 from ~10^3 to below 10^−15 within ~100–200 data passes; all SGD variants flatten at higher error levels. Claimed evidence of effective variance reduction and convergence.
- 8.2 MLP on MNIST:
  - Setup: Pretrain 3-layer MLP on digits 0–4; fine-tune to classify digits 5–9; rank r = 1; epochs = 50; optimizer AdamW (β1 = 0.9, β2 = 0.999, ε = 10^−8); lr = 2×10^−4; batch = 128; 20 independent seeds; report median accuracy ± std; select best median for Bernoulli-LoRA. Code: https://github.com/IgorSokoloff/Bernoulli-LoRA_experiments.
  - Results (Table 3):
    - FPFT: 99.5 (54,700 trainable).
    - LoRA: Gaussian/Zero 85.69 ± 1.60 (1K); Zero/Gaussian 89.82 ± 0.90 (1K).
    - COLA: Gaussian/Zero 93.32 ± 0.50 (1K); Zero/Gaussian 96.55 ± 0.20 (1K).
    - AsymmLoRA: Gaussian/Zero 64.04 ± 6.90 (133); Zero/Gaussian 74.52 ± 7.20 (912).
    - RAC-LoRA: Gaussian/Zero 93.02 ± 0.50 (133); Zero/Gaussian 96.49 ± 0.20 (912).
    - Bernoulli-LoRA: 96.46 ± 0.17 with 𝒟_A = Zero (deterministically fixed at initialization for better performance), 𝒟_B = Gaussian; p = 0.99 ⇒ expected trainable parameters ≈ 904 (p·912 + (1−p)·133).
  - Discussion (as stated): Bernoulli-LoRA matches RAC-LoRA test accuracy with comparable parameter efficiency and theoretical guarantees; both train one matrix per LoRA block; COLA trains two.
- Additional experimental details (Appendix E):
  - Full gradient setting (Figures 2a/b): Bernoulli-LoRA-GD and RAC-LoRA-GD exhibit similar convergence across p, for r = 1 and r = 2; γ = c/L̂ with c ∈ {1, 2}; higher r accelerates convergence (consistent with r/n factor).
  - Hardware: Python 3.10 on CPUs (AMD EPYC 7702 64-Core; Intel Xeon Gold 6148 2.40 GHz; Intel Xeon Gold 6248 2.50 GHz).
  - Stopping: median over 20 seeds; termination when ||∇f(x^t)||_2^2 ≤ 5×10^−16 or max iterations.

# Conclusion
- Impact statement provided: goal is to advance ML; no specific societal consequences highlighted beyond general potential impacts. No separate concluding claims on limitations or future work provided in this section.

# Appendix
- A: Basic inequalities (tower property, CBS, variance decomposition, Jensen) used in proofs.
- B: Assumption 1 discussion. Lemma 1 (rotational invariance implies scalar matrix). Lemma 2: For Gaussian sketches, E[H_B] = E[H_A] = (r/n) I_n, hence λ_min = r/n.
- C: Detailed proofs for core variants:
  - C.1 Bernoulli-LoRA-GD: lemma showing projected GD form; Theorems for non-convex (rate 2Δ^0/(γ λ_min^p T)), PL (linear rate (1 − γ μ λ_min^p)^T Δ^0), and non-smooth convex (Algorithm 3 with subgradient form and rates in Theorem 10).
  - C.2 Bernoulli-LoRA-SGD: Theorems 11 (non-convex) and 12 (PL) with weighted sampling analysis; explicit stepsize conditions and weighted iterate selection.
  - C.3 Bernoulli-LoRA-MVR: Lemmas 5–6 for descent and estimator recursion; Theorems 13 (non-convex) and 14 (PL) with Lyapunov function Φ_t incorporating estimator error; stepsize depends on L, b, λ_max^p.
  - C.4 Bernoulli-LoRA-PAGE: Lemma 7; Theorems 15 (non-convex) and 16 (PL) with Φ_t including 1/q factor; stepsize depends on L, q, λ_max^p.
- D: Federated extensions:
  - D.1 Fed-Bernoulli-LoRA-QGD: Unbiased compressor definition (E[𝒬(W)] = W, E||𝒬(W) − W||^2 ≤ ω||W||^2); assumption on client dissimilarity (Δ* ≥ 0); Lemma 8 shows expected smoothness constants A1 = L ω/M, B1 = 1, C1 = 2L ω Δ*/M; Theorems 17 (non-convex) and 18 (PL) with rates and stepsize ranges in terms of ω/M and λ_min^p, λ_max^p.
  - D.2 Fed-Bernoulli-LoRA-MARINA: Lemma 9 bounds estimator error using ω and L; Theorems 19 (non-convex) and 20 (PL) with stepsize depending on √(λ_max^p·(1−q)/q·ω/M).
  - D.3 Fed-Bernoulli-LoRA-EF21: Contractive compressor definition with parameter β (E||𝒞(W) − W||^2 ≤ (1−β)||W||^2); Lemma 10 per-client recursion with √(1−β); Theorems 21 (non-convex) and 22 (PL) with stepsize depending on √(λ_max^p(1−β)/(1−√(1−β))).
- E: Experimental details (data generation via sklearn make_regression with specified parameters; scaling; stopping criteria; hardware; figures).

# References
- Cited works include LoRA [Hu et al., 2022], PAGE [Li et al., 2021], MVR/STORM [Cutkosky and Orabona, 2019], MARINA [Gorbunov et al., 2021], EF21 [Richtárik et al., 2021], RAC-LoRA [Malinovsky et al., 2024], COLA [Xia et al., 2024], surveys and optimization texts (e.g., Beck, Nesterov), compression literature (QSGD, TernGrad, Natural Compression), and supporting theory on smoothness and PL conditions. Quantitative constants and assumptions are specified in the main text (e.g., ω, β, b, q, L, μ).